package com.example.fittrack.model;

public enum Role {
    USER
    // TODO: add ADMIN here when implementing admin role
    // will be used for seeding exercises through UI instead of seed script
}
